import { DirectiveTestDirective } from './directive-test.directive';

describe('DirectiveTestDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveTestDirective();
    expect(directive).toBeTruthy();
  });
});
