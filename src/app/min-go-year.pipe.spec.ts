import { MinGoYearPipe } from './min-go-year.pipe';

describe('MinGoYearPipe', () => {
  it('create an instance', () => {
    const pipe = new MinGoYearPipe();
    expect(pipe).toBeTruthy();
  });
});
