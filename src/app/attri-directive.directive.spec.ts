import { AttriDirectiveDirective } from './attri-directive.directive';

describe('AttriDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AttriDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
